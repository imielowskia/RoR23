
<%= form_with(model: course) do |form| %>
  <% if course.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(course.errors.count, "error") %> prohibited this course from being saved:</h2>

      <ul>
        <% course.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="mb-3">
    <label class="form-label">New course</label>
    <div class="input-group">
      <span class="input-group-text" id="inputGroup-sizing-default"><%= form.label :nazwa, style: "display: block" %></span>
      <%= form.text_field :nazwa, class: "form-control" %>
      <%= form.submit 'Zapisz',data: {turbo_frame: "Courses" }, class: "btn btn-sm btn-primary" %>
    </div>
  </div>

<% end %>

