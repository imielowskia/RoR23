<p style="color: green"><%= notice %></p>

<h1>Courses</h1>
<%= turbo_frame_tag "Courses" do %>
<table class="table table-sm">
  <thead>
  <tr>
    <th><strong>Nazwa</strong></th>
    <th></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
        <% @courses.each do |course| %>
            <%= render course %>
        <% end %>
  </tbody>
</table>

<div>
  <%= turbo_frame_tag "CourseNew" do %>
    <%= render "form", course: Course.new %>
  <% end %>
</div>
<% end %>
