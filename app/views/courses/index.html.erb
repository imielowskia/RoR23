<p style="color: green"><%= notice %></p>

<h1>Courses</h1>

<table class="table table-sm">
  <thead>
  <tr>
    <th><strong>Nazwa</strong></th>
    <th><strong>Zapisane grupy:</strong></th>
    <th></th>
    <th></th>
  </tr>
  </thead>
  <tbody>
        <% @courses.each do |course| %>
            <%= render course %>
        <% end %>
  </tbody>
</table>

<div>
  <%= render "form", course: Course.new %>
</div>

