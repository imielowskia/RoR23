<%= form_with(model: student) do |form| %>
  <% if student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>


  <div class="mb-3">
    <label class="form-label">New student</label>
  <div class="input-group">
    <span class="input-group-text"><%= form.label :indeks, style: "display: block", class: "" %></span>
    <%= form.text_field :indeks, class: "form-control form-control-sm" %>
  </div>
  <div class="input-group">
    <span class="input-group-text"><%= form.label :imie, style: "display: block" %></span>
    <%= form.text_field :imie, class: "form-control form-control-sm" %>
  </div>
  <div class="input-group">
    <span class="input-group-text"><%= form.label :nazwisko, style: "display: block" %></span>
    <%= form.text_field :nazwisko, class: "form-control form-control-sm" %>
  </div>

  <div class="input-group">
    <%= form.collection_select :group_id, Group.all, :id, :nazwa, {prompt: "Wybierz grupę"}, {class: "form-select form-control-sm"} %>
  </div>


    <%= form.submit "Zapisz", class: "btn btn-sm btn-primary" %>
  </div>
<% end %>
